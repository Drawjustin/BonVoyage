http{
    server {
        listen 80;

        # 웹 루트 디렉터리 설정
        root /usr/share/nginx/html;

        # 기본 페이지 설정
        index index.html;

        # 기본 페이지로 리다이렉션
        location / {
            try_files $uri $uri/ =404;
        }
    }
}
